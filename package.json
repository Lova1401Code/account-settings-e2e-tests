{
  "name": "account-settings-e2e-tests",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Tests E2E Playwright pour account-settings",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:report": "playwright show-report",
    "test:responsive": "playwright test --project=responsive-mobile --project=responsive-tablet"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "dotenv": "^17.2.3"
  }
}
